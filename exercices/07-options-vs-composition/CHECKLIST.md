# Checklist — Options → Composition API

- [ ] `UserListOptions.vue` en Options API (`defineComponent`)
- [ ] `data()`, `computed`, `methods`, `watch` fonctionnels
- [ ] `UserListComposition.vue` en `<script setup>`
- [ ] Même comportement exact
- [ ] Composant parent affiche les deux côte à côte
- [ ] Interface `User` dans `types.ts`
- [ ] Types explicites partout
- [ ] Zero `any`
